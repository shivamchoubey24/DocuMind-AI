# DocuMind-AI
A Streamlit-based multi-PDF chatbot that uses Retrieval-Augmented Generation (RAG) — FAISS vector search + HuggingFace embeddings + OpenAI GPT — to let users upload multiple PDFs and have a natural, context-aware conversation with them.
